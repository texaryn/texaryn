---
"@texaryn/core": minor
"@texaryn/schema-json": minor
"@texaryn/react": minor
---

First public alpha: JSON Schema to React form pipeline.

Schema evaluation, IR compiler, headless runtime, React bindings, default widgets, and prop getters.
