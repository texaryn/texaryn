---
'@texaryn/core': patch
---

The npm description leads with JSON Schema forms and schema-driven UI and names
the packages core is used with: `@texaryn/schema-json` and a React, Vue or Web
Components binding. The README opening line matches it, and the keywords add
`json-schema-form`, `dynamic-forms` and `form-generator`. No code changes.
