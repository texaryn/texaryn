---
---

Catalogues the twelve form-projection capabilities the JSON Schema support guide claimed but the manifest never declared, and generates that guide's support table from the manifest so the two cannot drift again. Touches `@texaryn/examples`, which is unpublished, plus the docs and playground apps. No package changes.
