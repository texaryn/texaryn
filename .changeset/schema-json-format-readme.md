---
'@texaryn/schema-json': patch
---

The README states when `format` is asserted: in Draft 7, including a schema
whose `$schema` is absent or unrecognized while the fallback is `draft-07`, and
never in 2019-09 or 2020-12, where declaring the format-assertion vocabulary
does not turn it on. No code changes.
