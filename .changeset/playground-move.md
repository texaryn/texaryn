---

---

No release. `packages/demo` moved to `apps/playground` and the private package was renamed to `@texaryn/playground`. The only published package touched is `@texaryn/react`, whose README link to the application had to follow the move, and a link fix does not warrant a version.
