{
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.20.0",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "vitest run --passWithNoTests",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "typescript": "~5.8.0",
    "vitest": "^3.0.0",
    "fast-check": "^4.0.0",
    "vue": "^3.5.0"
  }
}
