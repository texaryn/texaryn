---
title: Texaryn
description: A framework-neutral runtime for schema-driven interfaces.
template: splash
hero:
  tagline: Describe a form once. Render it with any framework, through any design system.
  actions:
    - text: What is Texaryn?
      link: /texaryn/start/what-is-texaryn/
      icon: right-arrow
    # The splash template hides the sidebar, so without this the landing page
    # offers no route to the playground at all.
    - text: Open the playground
      link: /texaryn/playground/
      icon: rocket
      variant: secondary
    - text: View on GitHub
      link: https://github.com/texaryn/texaryn
      icon: external
      variant: minimal
---

import { Card, CardGrid } from '@astrojs/starlight/components'

<CardGrid>
  <Card title="Schema in, UI out">
    A JSON Schema becomes a projection, a typed UI document, then a rendered
    form. Each stage is inspectable and none of them knows about React.
  </Card>
  <Card title="One form, many renderers">
    The same document renders through the default widgets, Bootstrap 5 or
    Material UI. Switching renderer changes presentation, not state.
  </Card>
  <Card title="Proven, not asserted">
    Every advertised capability carries an executable example, and the build
    fails when one is claimed without a demonstration.
  </Card>
  <Card title="Framework neutral by construction">
    The runtime is plain TypeScript. React is one binding over it rather than
    an assumption baked into the core.
  </Card>
</CardGrid>
